import { Images } from "./images.model";

export class Destination {
    constructor(public description: string, public distance: string, public images: Images, public name: string, public travel: string) {}
  }
  